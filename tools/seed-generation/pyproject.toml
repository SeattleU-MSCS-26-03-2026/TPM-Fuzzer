[project]
name = "seed-generation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "protobuf>=7.34.1",
]

[dependency-groups]
dev = [
    "ipdb>=0.13.13",
]
